# CUDA Graph engine follow-up preregistration — 2026-09-08

User authorized recommended optimization sequence. Scope: E18a 16 residual blocks, width76,34 matrices,192432 weights,A8/ReLU. Saved-trained E18a seeds0/1/2, each100 measured updating intervals. Three disposable warmup intervals then exact reset. No test evaluation, no12000-run, no extra precision/architecture/hyperparameter search. TDT settings unchanged:K64,b16,batch128,threshold8,max1fire,C8/leak1/reset,S.02/EMA.1/floor1e-5. Reference is existing gpu_graph, newly timed on same machine. Old frozen source files remain unchanged.

Five independent conditions, each relative to gpu_graph (no combination search):
- gpu_graph: original implementation.
- cpu_compact: recommendations1-3 as one CPU bookkeeping change: generate candidates/batches/uniforms once using original RNG call order; reuse schedule for original voting/selection operations; operate on selected16 coordinates instead of fullN candidate/counter tensors; reconstruct full-state zero-inclusive diagnostics. GPU evaluator unchanged.
- persistent_candidates: original CPU path, persistent128xN effective candidate weights; restore old perturbed coordinates from current base before patching new ones. Handle accepted base changes including coordinates outside previous indices; assignment of code*scale, never FP32 delta accumulation. Same BMM kernels.
- transfer_buffers: original CPU path; pinned fixed metadata/output buffers and reusable CUDA events; caller supplies accepted coordinate update to avoid evaluator full-weight comparison; combine synchronization/update and fixed input upload with safe completion before buffer reuse. Same GPU forward.
- fused_graph: original CPU path; torch.compile Inductor fusion of candidate parallel forward (CUDA Graph internal compile capture disabled; manualGraph retained). FP32/TF32-off remain fixed. Compilation/setup recorded separately. Numerical differences are reported, not optimized away by condition/precision tuning. Unsupported compilation is a recorded failed condition, no silent eager fallback.

Validation before measured runs: initial+saved-trained ×3 seeds;3 consecutive updates per condition against originalGraph. Retain losses128, selected coordinates, votes and counter states at all64 times, accepted action,S,RNG states. Required structural checks: same initial weights, schedule/indices/batches/codes/RNG, legal weights, finite outputs, correct buffer refresh. Exact-expected cpu_compact/persistent/transfer must match originalGraph candidate loss bits, all votes/counters/actions,S and final weights; any mismatch is fixed before measurement, with implementation debugging recorded. Diagnostics compared separately to identify aggregation-only floating rounding differences. Fused numerical criterion maxrelative loss<1e-5 plus votes/counters/action exact on common fixed states; report all failures and full100-step divergence, even if not accepted. Fixed-state comparison continues on baseline weights after divergence to avoid mixing model drift with kernel differences.

100-step benchmark every condition/seed independently evolves from saved E18a state. Conditions rotated byseed, sequential on oneGPU,CPUthreads1 affinity15. End-to-end timer includes CPU schedule/decisions, transfer,GPU, accepted update notification, model update; excludes disk output/setup/warmup. CUDA-event interval excludes host preprocessing and separately identified metadata transfer when possible. Report means/sampleSD, speedup vs newlytimedGraph, memory allocated/reserved/RSS, setup and first divergence, final weights/S/RNG. Store all128 candidate losses and64 absdiffs perstep for audit outside timed loop. No GPU-only speed quoted as whole-interval speed. Do not infer testaccuracy from100steps.

Audit: source snapshots and source/model/data SHA256; config/environment/runtime_workers; perseed/aggregate/validation CSV; lossesNPY, finalweights/RNG checkpoints; independent audit reconstructs decisions from recorded losses/candidate schedule and checks final states. All conditions including failed/inexact/slow ones reported. Do not auto-launch previous stoppedCPU job or fulltraining.
