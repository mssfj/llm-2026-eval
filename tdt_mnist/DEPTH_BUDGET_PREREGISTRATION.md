# Depth versus update budget — preregistration, 2026-09-08

User requests E17a architecture d76,A8,ReLU,pre-norm residual,CUDA Graph CPU-compact engine. Fresh seeds0/1/2. 8 blocks(18 matrices,100016 weights):6000/12000/24000/48000 intervals.32 blocks(66 matrices,377264 weights):6000/12000/24000/48000/96000 intervals;96000 added explicitly by user. Six independent depth/seed trajectories,27 predetermined checkpoint outcomes,not27 independent seeds. Reuse prefixes within a trajectory because the transition rule has no horizon dependence. No old run continuation and no additional budget/architecture search.

Unchanged MNIST90-dimensional preprocessing,train10000/val1000/test10000,data_seed0;ternaryINT8 initialzero1/3,gain1,fixedscale1/sqrt(fanin*2/3),no bias/latentFP32/backprop/STE;A8 absmax/127,FP32matmul/stream/loss;RMSNorm eps1e-8,no gain. K64,b16,batch128,threshold8,max1fire,C8,leak1,reset,Sinitial.02,EMA.1,floor1e-5. ProposalGenerator seed+1,batchGenerator seed+100000. Use frozen gpu_graph_optimizations cpu_compact (not fused/persistent/transfer conditions),CPUthreads1,affinity15,RTX5090,TF32false,deterministic algorithms,CUBLAS_WORKSPACE_CONFIG=:4096:8.

Validation initially and every500intervals; no earlystopping or condition selection. Save immutable models at every requested budget, checkpoint with model/S/RNG every500. ALL six trajectories must finish and endpoint model hashes be frozen BEFORE any test evaluation. Then evaluate each of27 fixed checkpoints once using original CPU evaluation protocol. Reuse saved test outcome if evaluation command resumes; do not retest. No test-conditioned changes. Historical E17a90.6366667%,E18c87.5533333% are context only because CPU/GPU trajectories differ. Main comparison uses both depths trained with the same CPU-compact GPU engine.

Report mean/sampleSD and paired seed differences at every shared budget;depth gap D(t)=test32(t)-test8(t). Plot accuracy vs intervals and actual cumulative fires/parameter, plus validation curves. Predeclared near-density comparison D_density=test32(48000)-test8(12000); weight ratio377264/100016=3.772036..., so4xsteps supplies6.05% more steps/parameter to deep model,notexactmatching. Additional comparisons32(96000)-8(24000),32(96000)-8(48000),within-depth gains12000→48000 (and32→96000). Report actualfirecounts/firefrac,uniqueupdatedcoordinatefraction,selectedcoordinates/parameter. Intervals are maxfirebudget,not guaranteed number of updates.

No invented binary success gate: if extra budget closes the gap at near-matched updates/parameter this is evidence consistent with dilution; if gap persists it suggests remaining optimization/architecture difficulty under tested budgets. Do not call either causal mechanism proved or exclude both coexisting. Increasing intervals also increases sampled minibatches and compute. Report RMS diagnostics alongside depth/budget effects. Warning criterion inherited:branch/streamratio>0.5 orlogitsRMS>10 is recorded,not automatic runfailure. No interpolation chosen bytest to claim exactmatching.

Diagnostics: full128candidate losses,64absdiffs,selected16coordinates,acceptedcoordinate/target,scale/counterstats,permatrix selection/fire events perinterval;coordinateupdate/selection totals;validation/RMS/quantization at500cadence;layer-isolated64pairs/16edges atinitial and requestedbudgets using existingCPUdiagnostic (separate time,never feeds training). Perseed configs,models/RNG,csv/npy;aggregate/pairedcomparisons;runtimeworkers/environment,sources/model/data SHA256; source frozen before fulltraining. Preflight:all sixinitialCPUval match E17a/E18c;cpu_compact matches originalGraph oninitial/savedtrained states forbothdepths/3seeds; initialshort100-step disposable timing. 8-blockfirst12000losses/fires/finalstate compared to savedGPU E17a without reading test.

Execution: sixGPUworkers sequentially,seed0:8→32;seed1:32→8;seed2:8→32. Stoponstructural/nonfinitefailure and retain partial logs;no silent retry/tuning. Save progress and trained prefixes. Automated postprocessing/audit/testing runs only after alltrainingcomplete. Audit reconstructs endpointweights from recordedactions,fullSsequence fromcandidate differences,selection/update totals and diagnostic rows;check source/data hashes. GPU/legacydecision replay atinitial and eachbudget checks local transition and RNG equality from frozen checkpoint states;these are diagnostic validation examples only. No full432000-interval naive rerun. No claim all RNG states independently replayed frominitial beyond sampledcheckpoints.
