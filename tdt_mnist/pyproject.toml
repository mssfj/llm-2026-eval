[project]
name = "tdt-mnist"
version = "0.1.0"
description = "Forward-only edge-evidence ternary learning on MNIST"
requires-python = ">=3.10"
dependencies = ["torch>=2.2", "torchvision>=0.17"]

[project.optional-dependencies]
plots = ["matplotlib>=3.7"]

[tool.uv]
package = false
