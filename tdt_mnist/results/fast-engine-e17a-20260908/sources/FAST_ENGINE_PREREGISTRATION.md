# E17a fast evaluation engine preregistration — 2026-09-08

No legacy source changes. Preserve train.epoch, candidate_pair, accumulate and select_actions (including RNG order, per-pair minibatches, tie-breaking and scale EMA). A wrapper evaluates an independently replayed schedule, then injects the ordered FP32 scalar losses into the original epoch code via an isolated function globals mapping. No module-global monkey patching.

Important implementation facts: legacy epoch draws a distinct 128-example minibatch for each of 64 pairs and samples 16 coordinates globally across matrices. A single shared 128-example batch or treating all later matrices as unperturbed would change the experiment. Cache one concatenated base forward over the 64 original batches (8192 examples); batch all 128 candidate suffixes (16384 examples); apply sparse corrections at every perturbed matrix, using current candidate inputs after the earliest affected matrix. Earliest matrix correction uses cached inputs and outputs. Algebraic equivalence is subject to FP32 ordering; no bitwise guarantee is claimed.

Level 1: trained E17a seed0 model, fixed 128 training examples, fixed candidate schedules; all 128 relative loss errors strictly below 1e-5 and exact votes, counters and firing. Include a global 16-coordinate block and per-matrix blocks, residual W1/W2 boundaries and repeated output rows. Report every discrepancy, do not change accuracy thresholds. An additional real-epoch schedule test checks RNG and original decision-path equivalence.

Level 2: three full fast E17a runs, seeds0/1/2, 12000 intervals and all original settings. Compare an independently replayed naive trajectory until first differing fired coordinate/target (or all intervals if identical), record 1-based first divergence. Validate naive replay against historical metrics before divergence. Strong pass only if all three firing sequences match completely. Otherwise pass iff final test mean in [90.337,90.937]%. Test is evaluated only after final training, never for selecting engine settings. Report failures and numerical versus implementation evidence.

Performance: CPU threads1, same host, naive and fast benchmark in separate sequential processes, 100 intervals at 8 and16 residual blocks, identical seeds/protocol, full128-example candidate losses. Warmup separate. Report interval times, extrapolated12000-interval time distinctly from actual reproduction runtime and historical naive actual runtime. RSS before workload, peak RSS, difference (allocator/process-inclusive temporary overhead), persistent tensor bytes and cache/activation estimates separately; do not equate RSS delta with exact live tensor allocation. Count MAC-weighted cached base cost (64 full minibatches, not1), suffix cost and requested idealized1+128*(L-l_min)/L separately, with zero-based l_min convention and inclusive recomputation explained. Parallel reproduction resource sharing recorded.

No test-driven optimization, new hyperparameters, legacy retraining as a reference experiment, STE, latent weights or gradient training. Numerical acceptance tests are empirical fixed-input checks, not a mathematical proof over all FP32 inputs. Freeze implementation hashes before full training.

## Numerical preflight amendment, before full runs / no test use
The requested unguarded low-rank engine failed saved-weight level1: max relative loss error 0.00076447695, with vote/counter discrepancies. A8 rounding discontinuities are the suspected amplifier. Preserve this failed implementation/report. The production `fast` path now flags any suffix A8 normalized value within1e-4 of a half-integer and recomputes flagged complete candidate losses through the original naive forward. This empirical guard passes the fixed-input tests but is not a universal FP32 equivalence proof. Preflight flagged all128 candidates in19 of20 cases; this can erase the speed benefit. Keep the threshold fixed. Report guard rate and performance honestly; do not claim successful acceleration if slower. An `unguarded` microbenchmark is diagnostic, not an extra accuracy-search condition.

For reproduction, when all128 losses are recomputed through the original loss function, the legacy decision path itself certifies equality to a naive interval from identical state/RNG; avoid running this identical computation a second time. At any interval with fewer than128 fallback losses, replay the naive interval independently while trajectories remain equal. Verify historical per-interval candidate differences, counters, scale, fires and matrix events; save actual fired coordinates/targets. Once a first firing divergence occurs, stop naive replay and retain its first-divergence evidence. This does not add a legacy reference training experiment.

To provide an actual full-run timing instead of only extrapolation, also time one12000-interval naive replay on the8-block structure, without test evaluation, as a performance workload. Run this and fast seed0 serially before fast seeds1/2 run concurrently. Compare the actual summed engine interval durations for the isolated naive workload versus isolated fast seed0; report total training wall time (validation/checkpoints) separately. This is timing/replay, not a new test-bearing accuracy condition. The short depth benchmarks remain100 intervals per engine/structure.
