# GPU CUDA Graphs E17a reproduction — preregistration,2026-09-08

User explicitly changed reference from E18 to E17. Use E17a:8 residual blocks,width76,18 matrices,100016 INT8 ternary weights,A8/ReLU,parameter-free RMSNorm eps1e-8,FP32 residual stream. Train fresh initial weights for seeds0/1/2,12000 intervals each. Reuse existing CPU E17a records; do not use saved-trained weights as initialization. Do not run E18/16-block full training.

Unchanged MNIST preprocessing/data split, data_seed0, train10000/val1000/test10000, CPU threads1, batch_seed=seed+100000, proposal RNG seed+1. K64 distinct128-example minibatches per pair,16 global coordinates,threshold8,max1fire,C8/leak1/reset,S0.02/EMA0.1/floor1e-5. CPU authoritative INT8 weights and original epoch decisions; fixed previously benchmarked GPU Graph candidate evaluator with ordinary full FP32 matrices/BMM,TF32 disabled,CUBLAS_WORKSPACE_CONFIG=:4096:8. No low-rank correction, numerical fallback, optimizer or precision tuning.

Primary accuracy reproduction criterion: final test3-seed mean in [90.337,90.937]%,using the fixed CPU reference90.637%. Report actual saved CPU mean90.6366667%,sample SD,paired seed deltas as well. Outside the band is a failure of the preregistered reproduction band; if above its upper limit, explicitly describe the improvement direction rather than labeling an accuracy degradation. No per-seed accuracy gate. Report first fired-coordinate/target divergence from independently replayed naive epochs, plus first numeric difference. The GPU arithmetic is already known not to be CPU-bitwise-identical; accuracy maintenance and bitwise equivalence are distinct claims.

Validation: CPU forward initial and every500 intervals, using the same evaluator/batch ordering as original E17. Final test: once per seed at12000 intervals using the original CPU inference/evaluation path for an identical measurement protocol, thereby isolating the change in training candidate evaluation. No additional GPU test or test-conditioned model selection. Test results are not read/aggregated until all3 final outcomes are fixed. No retries or condition changes based on validation/test.

Diagnostics: every interval128 candidate losses,64 absolute pair differences, selected coordinates, action coordinate/target, counter stats and histogram, scale, matrix events, CPU/GPU timing; initial/every500 CPU validation signals,A8 errors,branch/stream RMS; initial/final CPU isolated probes (identical diagnostic definition to E17). CPU diagnostics concern the learned weights and are explicitly distinguished from the GPU candidate arithmetic. Checkpoints with weight/RNG/scale every500. Fresh source snapshots,config,GPU environment,baseline/data SHA-256,per-seed manifests,aggregate/paired CSV,summary and independent audit.

Run3 workers sequentially on oneRTX5090,CPU affinity15,torch threads1; existing CPU-only experiments may continue and resource overlap must be recorded. Report actual engine/training times and setup/diagnostic costs separately; historical CPU vs new runtime is not a dedicated same-load speed benchmark. No need to stop or change the existing CPU experiments.
